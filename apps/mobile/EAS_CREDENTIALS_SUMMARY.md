# EAS Credentials Summary — BDT Connect

_Generated 2026-06-01 during account-side setup. EAS account: `iket721`._

## EAS project
- **Linked:** `@iket721/bdt-connect`
- **projectId:** `1f285fac-f332-43f2-9ee5-27e7f1c5caa2` (matches `app.json`)
- `eas init` / `eas project:info` verified — slug + id match, no fix needed.

## Apple account (developer.apple.com / App Store Connect)
- **Team:** BDT Talent Group, LLC
- **Apple Team ID:** `HZVNY29Y5U`  → set in `eas.json` (`submit.*.ios.appleTeamId`)
- **App ID registered:** `com.bdt.connect` (name "BDT Connect"), **Push Notifications** capability enabled.
- **App Store Connect app id (ascAppId):** `6775680378`  → set in `eas.json` (`submit.*.ios.ascAppId`)
- ⚠️ **`eas.json` `submit.*.ios.appleId` still `REPLACE_ME`** — fill with the Apple ID **email** you sign in with.

## Android credentials (EAS-managed) — ✅ already in EAS
- **Upload keystore:** alias `b6148dbf5e7021e5b05dfcdde78aa422`, type JKS (stored in EAS, not local), created 2026-05-30.
  - SHA-1:   `27:A3:82:87:E3:2E:46:2C:07:E8:ED:92:35:B1:70:A8:D7:E8:E2:61`
  - SHA-256: `AD:49:A6:9B:66:73:16:45:83:D6:26:36:50:74:D0:F6:8F:57:E9:F7:DA:B8:A5:5D:E2:AD:9C:4D:EC:BF:7D:00`
  - (also saved to `.firebase-sha-fingerprints.txt`, gitignored)
- **FCM V1 service account key:** ✅ already uploaded to EAS as a service credential
  (Firebase project `bdt-connect`, `firebase-adminsdk-fbsvc@bdt-connect.iam.gserviceaccount.com`), 2026-05-30.

## iOS credentials (EAS-managed) — ⏳ NOT YET GENERATED
EAS shows **no Apple credentials** for the project yet (Distribution Certificate,
Provisioning Profile, APNs push key). These are generated by `eas credentials`
(or auto-generated on the first iOS build) and require a live Apple login + 2FA.

Per `IOS_LAUNCH.md` §4/§7, iOS push uses the **EAS-managed APNs key** (no Firebase
on iOS, no `GoogleService-Info.plist`). To generate, from `apps/mobile/`:

```bash
eas credentials --platform ios
# → production profile → let EAS generate: Distribution Certificate,
#   Provisioning Profile (com.bdt.connect), and Push Key (APNs .p8)
```

(Apple login + 2FA required. The App ID `com.bdt.connect` already exists, so EAS
will attach to it rather than creating a new one.)
